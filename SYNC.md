# 资料库同步（第一阶段）

本阶段同步 Markdown、图片、文件名、资料夹、归属和排序。收藏、最近阅读、阅读位置和阅读偏好只保存在本机。用户在两端分别选定同一个 iCloud Drive 文件夹后才开启连接；工程不使用 CloudKit 或应用专用 iCloud 容器。

## 数据流与格式

阅读器始终读取 Application Support 下的本机 `Collections`。编辑先本地保存，再由独立 actor 记录变化并发送；新下载内容全部校验、暂存后才替换本机视图。连接通过系统文件选择器获得授权，以 security-scoped bookmark 恢复访问。远端访问使用 NSFileCoordinator，NSFilePresenter 的变化通知、前台轮询和手动操作触发更新。

共享资料库由 App 管理，布局为：

```text
StudyReader-library.json        # 格式版本和资料库 UUID
Changes/<event UUID>.json       # 不可变的增量操作记录
Files/<SHA256>.md               # 保持普通 Markdown 格式的内容版本
Files/<SHA256>.<图片扩展名>       # 内容寻址的附件
```

通过 App 导入、编辑和整理资料；直接修改共享目录内的记录或带哈希的版本文件不属于支持的编辑方式。原文件名和相对目录由操作记录保存，本机副本恢复为正常文件名，并保持 Markdown 图片相对路径。首次连接保留备份；不会直接把旧的 `library-organization.json` 或 `reading-state.json` 当成共享文件反复覆盖。

## 合并规则

- 每次提交有设备 UUID、唯一事件 UUID 和 Lamport 计数。记录是追加的，离线双方不会覆盖同名整库快照。
- 文档身份使用原来的 UUID；正文和文件名变化记录其已知父版本。顺序到达不影响结果；缺少父记录时等待，不提前应用删除。
- 同一文档出现互不包含的版本时，确定地选择一个主版本，其他正文保存为具有稳定 UUID 的「冲突副本」。副本成为独立文章，后来编辑原文不会自动消失；可以正常删除副本。
- 删除使用 tombstone。删除与未见过的编辑并发时，原文章删除，未见过的编辑保存为冲突副本。
- 资料夹名称、每篇文章归属、每个资料夹的排序、全局文章排序和资料夹排序分别合并。相同排序发生并发修改时按 Lamport 计数、设备 ID、事件 ID 确定结果；新建的资料夹和文章不会因为不在旧排序数组里而消失。
- 首次连接时，对同一导入包内路径、内容和附件完全相同的文章建立 UUID 映射，避免两台设备的样例和相同导入包重复。映射保留本机已有收藏和阅读位置。
- 重命名仍只改文件名，正文一级标题不会参与命名。

## 离线、恢复与性能

本机 `sync-state.json` 同时持久化待发送事件和对应的本机基线；`SyncCache/Files` 保留不可变内容。进程退出、网络失败后可重试，不把远端暂时缺失当作删除。下载校验大小、SHA-256、扩展名、路径及符号链接边界；发现损坏时保留正在使用的本机资料。

需要安装新内容时，先建立 `.sync-install-<UUID>` 暂存目录，再写 `.sync-transaction.json`。依次安装完整的内容、整理记录和同步基线；启动先按日志完成中断的安装，再读取书架，避免半份资料触发启动清理。单纯整理变化只安装两个元数据文件，不复制文章和图片。Markdown 保持独立副本；不在 App 中修改的图片使用只读硬链接共享本机缓存，文件系统不支持时回退到复制，避免多篇文章重复占用同一批附件的空间。大批导入按记录大小分批提交，每批分别更新正确的版本父节点。空闲检查不生成新操作，也不重读已验证的正文缓存。

首次手动连接会复制 `Collections`、整理记录、阅读状态及已有同步基线到 `Backups/BeforeSync-<UUID>/`。断开只移除连接信息；当前本机资料、待发送的本地修改和共享文件夹都保留。

远端和本机同步缓存暂不进行历史版本垃圾回收，避免离线设备缺少父版本。大量或频繁改写大附件时空间会增长；后续压缩需有完整的版本/删除迁移协议。

## 验证与边界

2026-09-17：16 项同步 XCTest 已在 Mac 和 iPhone 模拟器通过，完整回归为 Mac 73 项、iPhone 模拟器 64 项。使用两个独立本机资料库和一个协调访问的临时共享目录模拟两端，不使用真实个人资料。

覆盖导入和相对图片、整理顺序、相同导入去重与本机进度迁移、文件改名与独立正文修改、并发编辑、删除和编辑冲突、冲突副本持久化/删除、离线队列重启、先到元数据后到正文、缺少父记录、内容损坏、路径边界、每个安装步骤中断恢复、连接/重启/断开和连接前备份。另验证无需手动刷新即可跨资料库更新、不同元数据到达顺序生成相同冲突记录、相同并发正文不额外复制，以及分批提交后的版本关系和共享图片 inode。

Mac 界面在独立 bundle / 测试资料库中验收：侧栏入口、创建/连接界面、系统文件夹选择器、创建空资料库、状态更新、退出后重新启动并通过 bookmark 恢复访问均已实际完成。没有连接或上传用户的真实 iCloud 资料。

上述测试不等同于实际 iCloud 跨设备端到端测试。仍需在真实 Mac/iPhone 上选择同一 iCloud Drive 文件夹，验证上传/下载延迟、设备断网重连、系统移除本地下载、账号切换和长期运行。iOS 后台暂停时不能承诺立即更新，恢复前台后继续检查。当前首次下载会等待本轮所需内容完整后再更新书架，不提供逐篇下载按钮。

下一阶段单独实现收藏与阅读进度合并，并验证不同屏幕、字号和正文版本下的位置恢复。

## 接口依据

- [Apple：目录访问与持久授权](https://developer.apple.com/documentation/uikit/providing-access-to-directories)
- [Apple：文档选择器和协调读写](https://developer.apple.com/documentation/uikit/uidocumentpickerviewcontroller)
- [Apple：NSFilePresenter](https://developer.apple.com/documentation/foundation/nsfilepresenter)
- [Apple：按需下载 iCloud 文件](https://developer.apple.com/documentation/foundation/filemanager/startdownloadingubiquitousitem(at:))
