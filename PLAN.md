# 个人 Markdown 学习阅读器：开发路径

状态：已创建 Mac / iPhone 原生工程和可运行的本地阅读原型，双端构建和自动测试通过。iCloud 同步尚未接入。

更新日期：2026-09-17。

目标：在 Mac 和 iPhone 上阅读由 AI 生成或自己导入的 Markdown 学习资料，首批内容以概率论为主。内容关联留待后续。

## 1. 第一版范围

第一版的完整目标如下；这些目标分阶段交付，不代表当前全部完成。当前进度见第 5、8 节。

- 导入单个 MD 或资料文件夹，保留目录结构和本地图片。
- 按文件夹浏览，显示文章内标题目录。
- 阅读中文、英文、列表、表格、代码、图片、数学公式。
- 搜索标题和正文；阅读页内查找。
- 收藏、最近阅读、恢复阅读位置。
- 调整字号、切换外观、选择复制文字、折叠答案。
- 离线阅读已下载的内容；同步 MD、附件、收藏和阅读位置。
- 导出原始 MD 与素材，保留内容可迁移性。

内容关联、知识图谱、刷题与判分、内置 AI、富文本编辑器不纳入第一版。普通网络链接交由系统浏览器打开；资料间的相对 MD 链接跳转留待后续。

## 2. 技术选择

推荐路线：Swift + SwiftUI 应用界面，使用 WKWebView 展示本地生成的正文。

```text
Mac 窗口 / iPhone 页面（SwiftUI）
                 ↓
资料库、搜索、收藏、阅读状态
                 ↓
阅读组件（WKWebView）
                 ↓
markdown-it + @mdit/plugin-katex + KaTeX
                 ↓
本地 MD、图片、渲染样式与数学字体
```

复用 Markdown 解析、数学语法解析和公式排版；自行实现文件管理、两端界面、阅读状态、同步，以及组件间的连接。

所有 JavaScript、样式、KaTeX 字体在构建时打包进 App，运行时不依赖 CDN 或开发服务器。Node.js 若用于打包，只参与开发构建。

| 方案 | 已核实的能力与约束 | 本项目决策 |
| --- | --- | --- |
| markdown-it + KaTeX + WKWebView | Markdown 与公式由独立库处理；数学插件支持美元和括号两类分隔符 | 主方案，先用样例验证公式、长文、选择复制和离线体验 |
| Textual | SwiftUI 原生内容呈现、文本选择、数学扩展；当前主分支最低 iOS 18 / macOS 15 | 原生候选，只有主方案遇到明确问题时再做对比验证 |
| MarkdownUI | 官方已宣布进入维护模式，新功能转向 Textual | 新项目优先评估前两项 |

渲染依赖已锁定：markdown-it 15.0.2、@mdit/plugin-katex 1.1.1、KaTeX 0.18.7。项目已包含离线构建产物及第三方、字体许可。原型已通过真实 WKWebView 的公式和本地图片集成测试；长文性能尚未测量。

KaTeX 支持数学语法的一个明确子集，不承诺支持完整 LaTeX 文档。导入中的不支持公式应显示可读的源文本和局部提示，不导致整篇文章空白。若真实资料需要的语法超出范围，再评估 MathJax 替换成本。

## 3. 内容规范尽量轻

普通 UTF-8 Markdown 可以直接导入，不要求 YAML 文件头、知识点 ID 或特定模板。资料夹本身就是模块，例如“概率论”“英语”。

第一版约定：

- 普通内容支持标题、段落、加粗、引用、列表、表格、代码围栏和相对路径图片。
- 行内数学支持 `$...$` 和 `\(...\)`；块级数学支持 `$$...$$` 和 `\[...\]`。
- 数学插件配置为同时接受两类分隔符；保留代码块和转义美元符号的原意。
- 公式在 Markdown 解析阶段识别，避免先处理 Markdown 导致反斜杠丢失。
- 如存在 YAML Front Matter，阅读器隐藏元数据区，未知字段保留原样。缺少文件头不影响阅读。
- 标题取第一个一级标题，缺少时使用文件名。
- 文章目录由标题层级生成。
- “折叠解答”作为阅读选项：按照 Markdown 标题结构识别“答案”“解答”“解析”，范围到下一个同级或更高层级标题。缺少这些标题时正常阅读。
- 原始 MD 保持原样，显示时的处理不写回正文。

导入方式采用“复制到 App 管理的资料库”。导入文件夹时保留目录和附件相对路径；单文件导入若缺少本地图片，在相应图片位置提示，仍允许阅读文字。已支持自建空资料夹、文章排序和跨资料夹移动；组内文章平铺显示，嵌套文件夹的树形导航尚未实现。

整理分组独立于原始导入包：移动时更新文档 UUID 的归属，原文与附件不搬移，避免相对路径失效和同名附件覆盖。旧资料自动迁移成原来的资料夹分组，新建空资料夹可持久保存。“全部资料”和各资料夹有独立的手动顺序，搜索结果排序保留隐藏文章的位置；最近阅读仍按时间排列。

当前完全重复的资料会跳过；修改后重新导入会保留成独立副本，不覆盖旧资料。后续再增加更新预览、匹配原文档和冲突处理。原型限制单篇 MD 5 MB、单张图片 25 MB、每次资料导入 200 MB，正文要求 UTF-8。

这让外部 AI、编辑器和 App 之间保持简单的文件接口。

## 4. 数据与同步

以下表格是同步阶段的目标架构。当前使用本机 Application Support 中的 MD / 附件文件、导入包清单 JSON、资料夹归属与顺序 JSON，以及阅读状态 JSON；阅读偏好存入 UserDefaults。尚未使用 SwiftData、CloudKit 或 iCloud 容器。原始内容、整理记录与阅读状态已分开，后续迁移时保留文档 UUID。

| 数据 | 正式来源 | 同步策略 |
| --- | --- | --- |
| 文章、图片和用户写的独立笔记 | MD 与附件文件 | iCloud Documents / iCloud Drive |
| 文档身份与资料库内路径 | 系统生成的轻量元数据，每篇一个 UUID | 与内容文件一起同步 |
| 全文搜索和目录缓存 | 从已下载内容生成的本地索引 | 每台设备重建，不同步数据库文件 |
| 收藏、阅读位置和阅读偏好 | 独立 SwiftData 状态库 | CloudKit 私有数据库 |
| 整理资料夹、文档归属及手动顺序 | 以文档 UUID 关联的组织记录 | 后续纳入 CloudKit 私有数据库，并处理并发移动与排序 |

文档 UUID 随资料库元数据保存，改名仍保留；原始 MD 无需增加自定义字段。路径和正文哈希用于定位与检测更新，不单独充当文档身份。

当前阅读位置保存“源文本块锚点 + 附近文本 + 块内相对位置 + 阅读进度”，恢复时优先匹配文本，其次匹配锚点，最后退回进度。同步阶段补充内容版本、更新时间和设备标识，并验证不同字号、屏幕尺寸及正文更新后的定位。

同步实施原则：

- 本地保存先完成，同步在后台进行；下载未完成的文件有可见状态。
- 首次发现云端文件不等于正文与图片已经下载；提供下载与离线保留操作。
- 同一份正文发生并发修改时保留冲突副本，不直接丢弃任何一方。
- 收藏和阅读位置按稳定 UUID 合并；同一文档的近期阅读位置采用明确的时间戳与设备标识规则，并接受最终一致性。
- SwiftData + CloudKit 模型从一开始遵守其约束；不依赖云端唯一约束去实现去重。
- 内容同步和状态同步会分别到达，界面应能处理“有收藏记录但正文尚未下载”等中间状态。
- 删除、重命名、重新导入必须覆盖离线后重连的情况。

## 5. 按可验收的里程碑推进

| 阶段 | 工作 | 完成标准 |
| --- | --- | --- |
| P0：环境与工程准备 | 完整 Xcode、Mac/iPhone 工程、基础导航、确认设备系统版本与签名；检查 iCloud 能力的账号前置条件 | Mac 与 iPhone 模拟器能启动同一套共享代码；iCloud 条件明确 |
| P1：阅读原型 | 集成本地渲染库，接入概率论样例，字号与外观、图片和公式、文本选择 | 两端离线呈现同一文档；复杂公式和表格可读；错误公式不影响全文 |
| P2：资料库 | 文件和文件夹导入、附件、目录、文档身份、最近阅读、导出 | 导入一套资料后重启仍能使用；导出正文与素材无丢失 |
| P3：日常阅读能力 | 标题/全文搜索、页内查找、收藏、阅读位置、答案折叠 | 从搜索到文章对应位置，关闭后恢复；字号变化后定位合理 |
| P4：完整同步 | 文件下载、离线保留、CloudKit 状态、冲突与删除处理 | Mac 导入的资料可在 iPhone 下载；两端收藏与阅读位置收敛；离线后恢复不丢内容 |
| P5：个人试用 | 真机阅读、长文与大图、低网速、大字号和 VoiceOver、导入导出回归 | 连续日常使用时阅读顺畅，发现的问题有明确修复结果 |

当前进度：

- P0：双端工程、构建和模拟器运行已完成；真机签名与 iCloud 账号条件待确认。
- P1：公式、图片、表格和离线资源已接通；窄屏布局、深浅色与选择复制仍需完整交互验收。
- P2：导入、原文及附件保留、文档身份、新建资料夹、文章排序和跨资料夹移动已实现；目前导出仅含单篇 MD，完整资料包导出和嵌套目录浏览待做。
- P3：搜索、页内查找、收藏、阅读位置、答案折叠已实现；搜索结果跳到正文匹配处、长期定位体验待完善。
- P4、P5：尚未开始。

下一阶段先用一个 MD 和一张图片确认签名、iCloud 容器、上传和另一设备下载可行，再实现完整同步体验。

P0–P2 得到可用的离线阅读版本；P3–P5 覆盖第一版全部需求。按验收结果推进，暂不承诺缺少设备与资料规模依据的工期。

## 6. 第一轮渲染验证

已准备 `samples/probability/阅读验收样例.md`，覆盖中英混排、表格、代码、标题、条件概率、贝叶斯公式、积分、分段函数、矩阵、多行推导与两类数学分隔符。

样例已加入本地 SVG 图片；独立测试覆盖错误公式、不可信 HTML、危险 URL、转义和答案折叠。剩余重点检查：

- iPhone 窄屏：正文不被长公式撑宽，长公式和表格只在自身区域横向滚动。
- 浅色/深色和大字号：文字、公式、图片说明可读。
- 跨段文字选择与复制；公式另提供源 LaTeX 复制入口。
- 渲染资源全部本地加载；飞行模式下已下载资料正常阅读。
- 修改字号、折叠答案、图片加载完成后，阅读位置不会大幅跳动。
- 先用正常大小与约 100 KB 的合成长文记录首屏时间和滚动表现，再根据实际资料确定体积上限。
- 支持键盘与屏幕阅读器访问目录和折叠控件；公式的 MathML 输出需要真机验证。

自动测试不替代完整交互验收。当前已验证渲染资源实际加载、公式数量和本地图片；长文性能、手势选择、读屏和真机操作仍未完成验证。

## 7. 实现边界

当前工程结构：

```text
StudyReader/App/              应用入口、Mac/iPhone 导航和搜索
StudyReader/Library/          文件导入、文档身份、收藏和阅读状态
StudyReader/Reader/           WebView、目录、字号、外观、单篇导出
StudyReader/Resources/Reader/ 打包的 JS、CSS、字体与许可
WebReader/                   Markdown 渲染和阅读交互源码
scripts/                     离线资源构建
tests/                       JS 测试和双端 Swift 集成测试
samples/                     首次运行的示例资料
```

搜索目前直接查询本机已载入的标题和正文，未单独建立搜索数据库。资料规模增大后再引入可重建索引；同步实现时增加 Sync 模块。

用少量明确接口隔离文件访问、正文渲染和阅读状态；只有一个实际渲染实现。导入内容作为数据处理，HTML 原文默认禁用，正文不能注入脚本，附件访问限制在资料库范围，外部链接交给系统处理。KaTeX 保持 `trust: false`。

优先验证的逻辑包括：相对附件路径、重复导入、文档身份、阅读锚点恢复和同步冲突。渲染外观需要两端实际查看；上游库的 Markdown/公式解析能力不重复自研。

## 8. 当前环境与下一项工作

已安装 `/Applications/Xcode.app`，版本 Xcode 27.0（27A266a），首次启动检查通过。全局命令行开发目录仍指向 CommandLineTools；构建命令通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` 选择完整 Xcode，没有修改全局配置。

已完成 Mac 与 iOS 模拟器构建，并在 iPhone 18 Pro 模拟器安装运行。JavaScript 的 7 项测试、Mac 上的 15 项 XCTest 及 iPhone 模拟器上的同套 15 项 XCTest 均通过。双端 WebView 测试还检查了样例正文没有横向溢出，详细命令见 README。

Mac 和模拟器的 Debug 构建已配置为本地签名，开启签名后的双端 XCTest 均通过；无需填写 Team 即可在 Xcode 运行。Mac 保持沙盒，已补齐 WebKit 所需的网络客户端权限及导入导出所需的用户选择文件读写权限。

未配置 Apple Developer Team、真机签名及 iCloud 能力；iPhone 真机与 Release 配置仍使用自动签名。本次界面自动检查遇到 ScreenCaptureKit 捕获失败（-3811），鼠标拖放仍待完整交互验收；已验证拖放数据、组织记录持久化和原生 WebView 实际加载。

接下来的工作：先在 Xcode 运行原型、导入一套真实概率论资料试读；完善完整资料包导出、嵌套文件夹浏览和搜索定位；配置签名后做最小跨设备同步实验，再推进 P4。

## 9. 官方资料与选型依据

- [markdown-it](https://github.com/markdown-it/markdown-it)：Markdown 解析与扩展机制。
- [KaTeX 数学插件](https://mdit-plugins.github.io/katex.html)：支持美元、括号分隔符以及混合模式。
- [KaTeX 支持的数学语法](https://katex.org/docs/supported.html)：用于确定公式支持边界。
- [KaTeX 配置](https://katex.org/docs/options.html)：错误回退、MathML 与信任选项。
- [Textual](https://github.com/gonzalezreal/textual)：原生候选，包含文本选择和数学扩展。
- [Textual 平台要求](https://github.com/gonzalezreal/textual/blob/main/Package.swift)：当前主分支要求 iOS 18 / macOS 15。
- [MarkdownUI](https://github.com/gonzalezreal/swift-markdown-ui)：维护状态及后继项目说明。
- [Apple iCloud 配置](https://developer.apple.com/documentation/xcode/configuring-icloud-services)：文件和结构化数据的同步服务。
- [Apple 文档同步](https://developer.apple.com/documentation/uikit/synchronizing-documents-in-the-icloud-environment)：下载状态与冲突处理。
- [Apple SwiftData 同步](https://developer.apple.com/documentation/swiftdata/syncing-model-data-across-a-persons-devices)：CloudKit 兼容模型要求。
